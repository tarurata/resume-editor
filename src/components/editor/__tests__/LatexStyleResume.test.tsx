import { LatexStyleResume } from '../LatexStyleResume';

describe('LatexStyleResume', () => {
    it('should be a valid React component', () => {
        expect(LatexStyleResume).toBeDefined();
        expect(typeof LatexStyleResume).toBe('function');
    });
});
